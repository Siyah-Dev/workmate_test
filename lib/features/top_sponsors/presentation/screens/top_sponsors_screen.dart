import 'package:flutter/material.dart';

class TopSponsorsScreen extends StatelessWidget {
  const TopSponsorsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Top Sponsers"),),);
  }
}